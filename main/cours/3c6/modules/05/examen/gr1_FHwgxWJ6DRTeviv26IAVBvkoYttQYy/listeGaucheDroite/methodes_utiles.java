// calcule un indice relatif à la chaîne de gauche
// à partir d'un indice relatif à la liste
protected int indiceGauche(int indiceListe);

// calcule un indice relatif à la liste
// à partir d'un indice relatif à la chaîne de droite
protected int indiceListeAPartirIndiceDroite(int indiceDroite);

// décider s'il faut un élément dans la chaîne de gauche
protected boolean doitAllerAGauche(int indiceListe);
